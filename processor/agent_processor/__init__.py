"""
processor/agent_processor

v2 Agentic After-sales Platform 的 Agent 处理层。

本阶段（Step 2）只落地最小的 Triage Agent：
    User Query → Triage Agent → TriageResult

后续阶段会在此基础上扩展：
    Triage → Retrieval Tool → Diagnosis → Tool → HITL → Ticket → Final Answer
"""
